import 'package:flutter/material.dart';

class table_header extends StatelessWidget {
  const table_header({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}