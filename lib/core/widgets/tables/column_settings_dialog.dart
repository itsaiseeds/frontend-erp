import 'package:flutter/material.dart';

class column_settings_dialog extends StatelessWidget {
  const column_settings_dialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}