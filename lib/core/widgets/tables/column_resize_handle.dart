import 'package:flutter/material.dart';

class column_resize_handle extends StatelessWidget {
  const column_resize_handle({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}