import 'package:flutter/material.dart';

class app_data_table extends StatelessWidget {
  const app_data_table({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}