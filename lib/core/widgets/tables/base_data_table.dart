import 'package:flutter/material.dart';

class base_data_table extends StatelessWidget {
  const base_data_table({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}