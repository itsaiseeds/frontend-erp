import 'package:flutter/material.dart';

class responsive_table_layout extends StatelessWidget {
  const responsive_table_layout({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}