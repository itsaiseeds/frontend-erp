import 'package:flutter/material.dart';

class bulk_action_buttons extends StatelessWidget {
  const bulk_action_buttons({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}