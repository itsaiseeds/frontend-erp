import 'package:flutter/material.dart';

class policy_details_dialog extends StatelessWidget {
  const policy_details_dialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}