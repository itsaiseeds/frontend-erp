import 'package:flutter/material.dart';

class profile_shimmer extends StatelessWidget {
  const profile_shimmer({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}