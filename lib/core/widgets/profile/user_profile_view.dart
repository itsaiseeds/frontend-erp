import 'package:flutter/material.dart';

class user_profile_view extends StatelessWidget {
  const user_profile_view({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}