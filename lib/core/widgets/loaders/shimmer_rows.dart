import 'package:flutter/material.dart';

class shimmer_rows extends StatelessWidget {
  const shimmer_rows({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}