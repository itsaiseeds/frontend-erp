import 'package:flutter/material.dart';

class linear_progress_bar extends StatelessWidget {
  const linear_progress_bar({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}