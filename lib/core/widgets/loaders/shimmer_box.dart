import 'package:flutter/material.dart';

class shimmer_box extends StatelessWidget {
  const shimmer_box({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}