import 'package:flutter/material.dart';

class three_dots_loader extends StatelessWidget {
  const three_dots_loader({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}