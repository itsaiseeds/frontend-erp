import 'package:flutter/material.dart';

class loading_button extends StatelessWidget {
  const loading_button({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}