import 'package:flutter/material.dart';

class module_header_buttons extends StatelessWidget {
  const module_header_buttons({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}