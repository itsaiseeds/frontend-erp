import 'package:flutter/material.dart';

class confirmation_dialog extends StatelessWidget {
  const confirmation_dialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}