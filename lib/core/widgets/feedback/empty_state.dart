import 'package:flutter/material.dart';

class empty_state extends StatelessWidget {
  const empty_state({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}