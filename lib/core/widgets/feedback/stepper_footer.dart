import 'package:flutter/material.dart';

class stepper_footer extends StatelessWidget {
  const stepper_footer({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}