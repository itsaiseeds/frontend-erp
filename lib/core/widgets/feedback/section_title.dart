import 'package:flutter/material.dart';

class section_title extends StatelessWidget {
  const section_title({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}