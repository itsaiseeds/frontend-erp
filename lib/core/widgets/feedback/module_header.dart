import 'package:flutter/material.dart';

class module_header extends StatelessWidget {
  const module_header({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}