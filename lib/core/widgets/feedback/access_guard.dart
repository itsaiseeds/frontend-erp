import 'package:flutter/material.dart';

class access_guard extends StatelessWidget {
  const access_guard({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}