import 'package:flutter/material.dart';

class dialog_step_header extends StatelessWidget {
  const dialog_step_header({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}