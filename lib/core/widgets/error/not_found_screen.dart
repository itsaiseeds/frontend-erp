import 'package:flutter/material.dart';

class not_found_screen extends StatelessWidget {
  const not_found_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}