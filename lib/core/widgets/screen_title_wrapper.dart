import 'package:flutter/material.dart';

class screen_title_wrapper extends StatelessWidget {
  const screen_title_wrapper({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}