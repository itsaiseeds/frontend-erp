import 'package:flutter/material.dart';

class app_date_picker extends StatelessWidget {
  const app_date_picker({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}