import 'package:flutter/material.dart';

class searchable_popup_menu_button extends StatelessWidget {
  const searchable_popup_menu_button({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}