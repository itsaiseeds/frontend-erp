import 'package:flutter/material.dart';

class custom_toggle_card extends StatelessWidget {
  const custom_toggle_card({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}