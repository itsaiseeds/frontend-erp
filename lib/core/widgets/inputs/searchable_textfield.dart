import 'package:flutter/material.dart';

class searchable_textfield extends StatelessWidget {
  const searchable_textfield({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}