import 'package:flutter/material.dart';

class bulk_delete_dialog extends StatelessWidget {
  const bulk_delete_dialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}