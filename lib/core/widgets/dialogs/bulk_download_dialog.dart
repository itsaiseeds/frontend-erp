import 'package:flutter/material.dart';

class bulk_download_dialog extends StatelessWidget {
  const bulk_download_dialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}