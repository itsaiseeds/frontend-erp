import 'package:flutter/material.dart';

class app_sidebar extends StatelessWidget {
  const app_sidebar({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}